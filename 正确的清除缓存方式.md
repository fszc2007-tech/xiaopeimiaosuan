# 正确的清除缓存方式

## ❌ 错误命令

```bash
npx expo run:ios --clear  # ❌ 不支持 --clear 参数
```

## ✅ 正确方式

### 方式 1: 手动清除缓存（推荐）

```bash
# 1. 停止当前进程（Ctrl+C）

# 2. 清除缓存
cd /Users/gaoxuxu/Desktop/小佩APP/app
rm -rf .expo node_modules/.cache

# 3. 重新启动
npx expo run:ios
```

---

### 方式 2: 分两步启动

```bash
# 终端 1: 启动 Metro（带清除缓存）
cd /Users/gaoxuxu/Desktop/小佩APP/app
npx expo start --clear

# 终端 2: 运行 iOS
cd /Users/gaoxuxu/Desktop/小佩APP/app
npx expo run:ios
```

---

## 🔄 完整重启流程

### Step 1: 停止所有进程

在运行 Expo 的终端中按 `Ctrl + C`

### Step 2: 清除缓存

```bash
cd /Users/gaoxuxu/Desktop/小佩APP/app
rm -rf .expo node_modules/.cache
```

### Step 3: 重新启动

```bash
npx expo run:ios
```

---

## 📋 命令说明

| 命令 | 说明 | 支持 --clear |
|------|------|-------------|
| `expo start` | 启动 Metro bundler | ✅ 支持 |
| `expo run:ios` | 构建并运行 iOS | ❌ 不支持 |
| `expo start --clear` | 启动 Metro 并清除缓存 | ✅ 支持 |

---

**现在请运行：**
```bash
cd /Users/gaoxuxu/Desktop/小佩APP/app
rm -rf .expo node_modules/.cache
npx expo run:ios
```

