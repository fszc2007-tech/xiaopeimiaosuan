# 启动状态检查

## ✅ 已完成

### 1. 数据库迁移 ✅
```bash
mysql -u root xiaopei -e "DESCRIBE users;" | grep -E "ai_calls|pro_plan"
```

**结果**：
```
pro_plan         enum('yearly','monthly','quarterly','lifetime')  YES    NULL
ai_calls_today   int                                              NO     0
ai_calls_date    varchar(10)                                      NO     ''
```

✅ 所有字段已存在，迁移已完成！

### 2. MySQL 数据库 ✅
```bash
brew services start mysql
```

**状态**：✅ 已运行

### 3. Core 后端服务 ✅
```bash
cd core && npm run dev
```

**状态**：✅ 已在后台启动

---

## 📋 下一步

### 1. 检查 Core 服务是否正常运行

打开浏览器访问：
- http://localhost:3000/health （健康检查）
- http://localhost:3000/api/v1/pro/status （需要登录）

### 2. 启动前端 App

```bash
cd app
npm start
```

### 3. 测试功能

#### 测试 AI 次数限制
1. 登录非会员账号
2. 进入聊天页面
3. 连续发送 5-10 条消息
4. 观察是否触发「今日解读次数已用完」对话框

#### 测试订阅页面
1. 点击「小佩服务」→「小佩 Pro 推荐」
2. 检查页面显示：
   - ✅ 当前状态卡片（显示今日解读次数）
   - ✅ 绿色介绍卡片（情绪价值）
   - ✅ 权益对比表（AI 解读 → 功能 → 命盘数量）
   - ✅ 订阅计划（月 39 / 季 99 / 年 348）
3. 选择订阅计划并点击「立即订阅」
4. 确认订阅成功并状态更新

---

## 🔍 故障排查

### Core 服务未启动

**检查进程**：
```bash
lsof -i :3000
```

**查看日志**：
```bash
cd core
npm run dev
```

### 数据库连接失败

**检查 MySQL 状态**：
```bash
brew services list | grep mysql
```

**测试连接**：
```bash
mysql -u root xiaopei -e "SELECT 1;"
```

### 前端无法连接后端

**检查 API Base URL**：
- 文件：`app/src/services/api/apiClient.ts`
- 应该是：`http://localhost:3000`

---

## 📝 路由名称确认

✅ **已修复**：`apiClient.ts` 中的路由名称

**修改前**：
```typescript
navigationRef.current.navigate('ProSubscription' as never);
```

**修改后**：
```typescript
const { SCREEN_NAMES } = require('@/constants/routes');
navigationRef.current.navigate(SCREEN_NAMES.PRO_SUBSCRIPTION as never);
```

**路由定义**：
- `app/src/constants/routes.ts`：`PRO_SUBSCRIPTION: 'ProSubscription'`
- `app/src/navigation/RootNavigator.tsx`：已正确注册

---

**状态**：✅ 数据库和后端服务已启动，可以开始测试！


