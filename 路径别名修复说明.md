# 路径别名修复说明

## ❌ 错误信息

```
Unable to resolve "@/constants/routes" from "src/navigation/RootNavigator.tsx"
```

## 🔍 问题原因

**Metro bundler 无法识别 TypeScript 路径别名**。

虽然 `tsconfig.json` 中配置了路径别名：
```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  }
}
```

但 **Metro bundler（React Native 的打包工具）需要单独的 Babel 配置**才能识别路径别名。

---

## ✅ 修复方案

### 1. 创建 `babel.config.js`

已创建文件：`app/babel.config.js`

```javascript
module.exports = function(api) {
  api.cache(true);
  return {
    presets: ['babel-preset-expo'],
    plugins: [
      [
        'module-resolver',
        {
          root: ['./'],
          alias: {
            '@': './src',
          },
          extensions: [
            '.ios.ts',
            '.android.ts',
            '.ts',
            '.ios.tsx',
            '.android.tsx',
            '.tsx',
            '.jsx',
            '.js',
            '.json',
          ],
        },
      ],
    ],
  };
};
```

### 2. 安装依赖

已安装：`babel-plugin-module-resolver`

```bash
npm install --save-dev babel-plugin-module-resolver
```

---

## 🔄 下一步操作

### 重要：需要重启 Expo 服务器

Babel 配置更改需要重启才能生效。

#### 方法 1: 如果正在运行 `expo run:ios`

1. **停止当前进程**：在终端中按 `Ctrl + C`
2. **清除缓存并重新启动**：
   ```bash
   cd /Users/gaoxuxu/Desktop/小佩APP/app
   npx expo run:ios
   ```

#### 方法 2: 如果正在运行 `expo start`

1. **停止当前进程**：在终端中按 `Ctrl + C`
2. **清除缓存并重新启动**：
   ```bash
   cd /Users/gaoxuxu/Desktop/小佩APP/app
   npx expo start --clear
   ```

---

## ✅ 验证修复

重启后，应该不再出现以下错误：
- ❌ `Unable to resolve "@/constants/routes"`
- ❌ `Unable to resolve "@/types/navigation"`
- ❌ `Unable to resolve "@/store"`

App 应该能正常启动。

---

## 📋 配置说明

### 路径别名映射

| 别名 | 实际路径 |
|------|---------|
| `@/constants/routes` | `src/constants/routes` |
| `@/types/navigation` | `src/types/navigation` |
| `@/store` | `src/store` |
| `@/screens/Auth` | `src/screens/Auth` |

### 支持的文件扩展名

- `.ts` / `.tsx`
- `.ios.ts` / `.ios.tsx`
- `.android.ts` / `.android.tsx`
- `.js` / `.jsx`
- `.json`

---

## 🎯 现在请操作

1. **停止当前运行的 Expo 进程**（`Ctrl + C`）
2. **重新运行**：
   ```bash
   cd /Users/gaoxuxu/Desktop/小佩APP/app
   npx expo run:ios
   ```

应该可以正常启动了！🚀

