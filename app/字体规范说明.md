# 小佩 App 字体规范说明

## 📱 系统字体

### iOS
- **英文**: San Francisco
- **中文**: 苹方 (PingFang SC)
- **说明**: 无需指定 `fontFamily`，React Native 自动使用系统默认

### Android
- **英文**: Roboto
- **中文**: 思源黑体 (Noto Sans CJK)
- **说明**: 无需指定 `fontFamily`，React Native 自动使用系统默认

---

## 📐 字号规范

| 用途 | 字号 | Token | 字重 | 行高 |
|------|------|-------|------|------|
| **Tab栏/底部导航** | 10 | `fontSizes.xxs` | regular | - |
| **次要说明/辅助文字** | 12 | `fontSizes.xs` | regular | - |
| **正文（信息型）** | 14 | `fontSizes.sm` | regular | 20-22 |
| **正文（舒适阅读）** | 16 | `fontSizes.base` | regular | 22-24 |
| **模块标题/卡片标题** | 18 | `fontSizes.lg` | semibold | - |
| **大标题/页面标题** | 20 | `fontSizes.xl` | semibold/bold | - |
| **页面主标题/Banner** | 22 | `fontSizes['2xl']` | bold | - |
| **特大标题** | 24 | `fontSizes['3xl']` | bold | - |
| **按钮文字（小）** | 14 | `fontSizes.sm` | semibold | - |
| **按钮文字（大）** | 16 | `fontSizes.base` | semibold | - |

---

## 🎯 实用方案

### 方案 A：信息工具型 App（推荐用于小佩）

```typescript
// 页面标题
fontSize: fontSizes.lg,      // 18
fontWeight: fontWeights.semibold,

// 正文
fontSize: fontSizes.sm,      // 14
fontWeight: fontWeights.regular,
lineHeight: fontSizes.sm * lineHeights.normal, // 21

// 辅助文字
fontSize: fontSizes.xs,      // 12
fontWeight: fontWeights.regular,

// 主按钮
fontSize: fontSizes.base,    // 16
fontWeight: fontWeights.semibold,
```

### 方案 B：内容阅读型 App

```typescript
// 页面主标题
fontSize: fontSizes['2xl'],  // 22
fontWeight: fontWeights.bold,

// 章节/模块标题
fontSize: fontSizes.lg,      // 18
fontWeight: fontWeights.semibold,

// 正文
fontSize: fontSizes.base,    // 16
fontWeight: fontWeights.regular,
lineHeight: fontSizes.base * lineHeights.normal, // 24

// 辅助说明
fontSize: fontSizes.xs,      // 12
fontWeight: fontWeights.regular,
```

---

## ⚠️ 重要原则

### ✅ 应该统一的
- 页面标题、按钮、卡片标题等通用 UI 元素
- 说明文字、提示文字、辅助信息
- 导航栏、Tab栏文字

### ❌ 不应该改动的（保持原有设计）
- **四柱表格**中的天干地支（如：甲、乙、丙、丁、子、丑、寅、卯）
- **藏干**显示（如：甲木、乙木）
- **十神**显示（如：正官、偏财）
- **纳音**显示（如：大林木）
- **神煞**标签
- **其他命理核心显示内容**

---

## 💡 使用示例

### 页面标题
```typescript
<Text style={{
  fontSize: fontSizes.lg,
  fontWeight: fontWeights.semibold,
  color: colors.ink,
}}>
  我的命盤
</Text>
```

### 正文
```typescript
<Text style={{
  fontSize: fontSizes.sm,
  fontWeight: fontWeights.regular,
  lineHeight: fontSizes.sm * lineHeights.normal,
  color: colors.ink,
}}>
  這是一段正文內容
</Text>
```

### 辅助说明
```typescript
<Text style={{
  fontSize: fontSizes.xs,
  color: colors.textSecondary,
}}>
  這是輔助說明文字
</Text>
```

### 按钮
```typescript
<Text style={{
  fontSize: fontSizes.base,
  fontWeight: fontWeights.semibold,
  color: '#ffffff',
}}>
  確定
</Text>
```

---

## 🔍 字重使用建议

| 字重 | 使用场景 | 不要用在 |
|------|----------|----------|
| **regular (400)** | 正文、常规文字 | 标题、按钮 |
| **medium (500)** | 需要稍微突出的文字 | 不常用 |
| **semibold (600)** | 标题、按钮、重要信息 | 正文 |
| **bold (700)** | 特别重要的标题 | 正文、辅助信息 |

**原则：不要所有地方都用加粗，会很吵。**

---

## 📏 行高建议

| 字号 | 建议行高 | 计算方式 |
|------|----------|----------|
| 14 | 20-22 | `fontSizes.sm * lineHeights.normal` (21) |
| 16 | 22-24 | `fontSizes.base * lineHeights.normal` (24) |
| 18+ | 根据需要 | `fontSize * lineHeights.tight` (标题) |

**原则：行高太小会显得挤、难读。**

---

## 🎨 与颜色的配合

```typescript
// 主标题
color: colors.ink,           // 黑色
fontWeight: fontWeights.semibold,

// 正文
color: colors.ink,           // 黑色
fontWeight: fontWeights.regular,

// 辅助说明
color: colors.textSecondary, // 灰色
fontSize: fontSizes.xs,

// 品牌色文字（链接、强调）
color: colors.primary,       // 绿色
fontWeight: fontWeights.medium,
```






