# Expo 公共环境变量（编译时注入）
# 注意：iOS 模拟器可以直接用 localhost
# 真机需要使用 Mac 的局域网 IP 地址
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000

# iOS 订阅 Mock 开关（仅开发模式有效）
# 1 = 开启 Mock（订阅走 /dev/force-pro，快速测试）
# 0 = 关闭 Mock（订阅走正式 API）
# 默认：1（开启 Mock 模式，方便测试）
EXPO_PUBLIC_MOCK_IOS_SUBSCRIPTION=1
